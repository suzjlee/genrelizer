<%= form_for(@sub_genre) do |f| %>
  <% if @sub_genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_genre.errors.count, "error") %> prohibited this sub_genre from being saved:</h2>

      <ul>
      <% @sub_genre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :parent_genre %><br>
    <%= f.text_field :parent_genre %>
  </div>
  <div class="field">
    <%= f.label :child_genre %><br>
    <%= f.text_field :child_genre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
